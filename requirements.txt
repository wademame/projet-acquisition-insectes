# requirements.txt
# Bibliotheques Python du projet d'acquisition d'images d'insectes
#
# Installation (dans le venv active) :
#   pip install -r requirements.txt
#
# Ces deux bibliotheques ne sont pas incluses dans Python par defaut.
# Tout le reste (tkinter, os, time, threading, subprocess, ctypes,
# datetime, sys) est integre a Python et ne necessite aucune installation.
#
# opencv-python : capture depuis la camera Jeulin + algorithme de focus stacking
# pillow        : affichage des photos dans l'apercu de l'interface Tkinter
 
opencv-python
pillow